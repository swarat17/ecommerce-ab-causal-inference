name: 'ab_testing'
version: '1.0.0'
config-version: 2

profile: 'ab_testing'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  ab_testing:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
